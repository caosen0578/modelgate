# Spec Me2AI · 02 · 系统架构设计

> **文档性质**：人工编写，AI 严格遵守，架构决策不得绕过

---

## 1. 技术栈约束（强制，不得替换）

| 层次 | 技术 | 版本 | 禁止替换为 |
|---|---|---|---|
| 语言 | Java | 17 | 不得降级至 JDK 8/11 |
| 框架 | Spring Boot | 3.3.5 | — |
| 网关 | Spring Cloud Gateway | 2023.0.3 对应版本 | 不得使用 Zuul |
| 持久化 | MyBatis Plus | 3.5.9 | **禁止引入 JPA/Hibernate/JOOQ，禁止使用原生 MyBatis XML Mapper** |
| 数据库 | MySQL 8.0 | — | — |
| 缓存 | Caffeine L1 + Redis L2 | — | 不得只用 Redis 单层 |
| 消息 | Kafka | — | 不得使用 RabbitMQ/RocketMQ 替换 |
| 熔断 | Resilience4j | 2.2.0 | 不得使用 Hystrix |
| 注册 | Consul | — | — |
| 配置 | Apollo | 2.3.0 | — |
| 构建 | Maven | — | 不得切换 Gradle |

---

## 2. 包结构（强制，不得新增顶级包）

```
com.pab.ficc.idp.modelgate
├── ModelGateApplication.java          启动类
│
├── filter/                            Gateway GlobalFilter（所有过滤器在此）
│   ├── AdminAuthFilter.java           order=-200，管理接口鉴权
│   ├── CredentialAuthFilter.java      order=-190，业务凭证鉴权（替换旧 AuthFilter）
│   ├── AppAccessControlFilter.java    order=-180，IP/域名白名单
│   ├── ModelAuthFilter.java           order=-170，模型授权校验
│   ├── RateLimitFilter.java           order=-160，QPS 限流
│   ├── TraceFilter.java               order=-150，traceId 注入
│   ├── ModelRouteFilter.java          order=-140，动态路由 + 参数转换
│   └── CircuitBreakerFilter.java      order=-50，熔断
│
├── adapter/                           上游协议适配器（Strategy 模式）
│   ├── ProviderAdapter.java           接口定义
│   ├── OpenAIAdapter.java             openai 规范（直透）
│   ├── AzureOpenAIAdapter.java        azure_openai 规范
│   ├── AnthropicAdapter.java          anthropic 规范
│   └── CustomAdapter.java             custom 规范
│
├── admin/                             管理接口鉴权相关
│   ├── AdminAuthFilter.java
│   ├── AdminAuthProperties.java
│   └── IpMatcher.java
│
├── controller/                        管理后台 REST Controller（WebFlux）
│   ├── ModelController.java           /admin/models/**
│   ├── AppController.java             /admin/apps/**
│   ├── CredentialController.java      /admin/apps/:id/credentials/**
│   ├── ModelAuthController.java       /admin/apps/:id/auth/**
│   ├── AccessRuleController.java      /admin/apps/:id/access/**
│   ├── GatewayConfigController.java   /admin/config/**
│   └── WhitelistAdminController.java  保留兼容（可重定向至新接口）
│
├── entity/                            业务实体（MyBatis Plus 注解：@TableName/@TableId）
│   ├── ModelInstance.java
│   ├── ModelCredential.java
│   ├── AppInfo.java
│   ├── AppCredential.java
│   ├── AppModelAuth.java
│   ├── AccessRule.java
│   └── GatewayConfig.java             已有，保留
│
├── mapper/                            MyBatis Plus Mapper 接口（替代 DAO 层）
│   ├── ModelMapper.java               extends BaseMapper<ModelInstance>
│   ├── ModelCredentialMapper.java     extends BaseMapper<ModelCredential>
│   ├── AppMapper.java                 extends BaseMapper<AppInfo>
│   ├── AppCredentialMapper.java       extends BaseMapper<AppCredential>
│   ├── AppModelAuthMapper.java        extends BaseMapper<AppModelAuth>
│   ├── AccessRuleMapper.java          extends BaseMapper<AccessRule>
│   ├── GatewayConfigMapper.java       extends BaseMapper<GatewayConfig>
│   └── AuditLogMapper.java            extends BaseMapper<AuditLog>
│
├── service/                           业务逻辑
│   ├── ModelService.java              模型 CRUD + 缓存管理
│   ├── AppService.java                接入系统 CRUD
│   ├── CredentialService.java         凭证生成/校验/缓存
│   ├── ModelAuthService.java          授权管理
│   ├── AccessRuleService.java         访问规则管理
│   ├── GatewayConfigService.java      已有，保留
│   ├── RateLimitService.java          已有，保留
│   └── AuditService.java              已有，保留
│
├── cache/                             缓存策略统一管理
│   └── GatewayCacheManager.java       统一的失效/刷新逻辑
│
├── crypto/                            密钥加解密
│   └── CryptoService.java             AES-256-GCM，密钥从环境变量读
│
├── apollo/                            Apollo 配置监听（已有）
│   └── DynamicConfigHolder.java
│
├── config/                            Spring 配置类
│   ├── JacksonConfig.java
│   ├── RedisConfig.java
│   └── KafkaConfig.java
│
├── common/                            通用层
│   ├── Result.java
│   ├── ErrorCode.java
│   ├── BusinessException.java
│   ├── GlobalExceptionHandler.java
│   └── TraceContext.java
│
└── metrics/                           Prometheus 指标
    └── GatewayMetrics.java
```

---

## 3. 过滤器执行顺序（强制，order 值不得随意更改）

```
请求进入
    │
    ▼  order=-200  AdminAuthFilter
       保护 /admin/** 和 /actuator/**，其他路径直接放行
    │
    ▼  order=-190  CredentialAuthFilter
       API Key → SHA256 hash → Caffeine L1 → Redis L2 → DB L3
       AK/SK → 取出 SK → 重算签名 → 比对 → 时间戳防重放(±5min)
       未通过 → 401
    │
    ▼  order=-180  AppAccessControlFilter
       从凭证关联的 app_id 加载访问规则（Caffeine 缓存）
       校验客户端 IP 是否匹配白名单
       规则为空 → 直接放行
       未通过 → 403
    │
    ▼  order=-170  ModelAuthFilter
       解析请求体中的 model 字段（从 path 或 body）
       查询 mg_app_model_auth（Caffeine 缓存）
       校验：系统开关 → 授权开关 → 授权是否过期 → 模型是否启用
       未通过 → 403
    │
    ▼  order=-160  RateLimitFilter
       Redis Lua 令牌桶，key=app_id，每秒滑动窗口
       超限 → 429
    │
    ▼  order=-150  TraceFilter
       生成 traceId（UUID 前16位），写入 MDC 和响应头 X-Trace-Id
       写入 X-Mg-App、X-Mg-Model 响应头
    │
    ▼  order=-140  ModelRouteFilter
       加载 ModelInstance + ModelCredential（Caffeine 缓存 30s）
       调用对应 ProviderAdapter 转换请求体和路径
       注入上游鉴权 Header
       设置 GATEWAY_REQUEST_URL_ATTR
    │
    ▼  order=-50   CircuitBreakerFilter
       Resilience4j，熔断器按 model_id 独立实例
    │
    ▼  Spring Cloud Gateway 路由转发
    │
    ▼  doFinally   AuditService（异步 Kafka）
```

---

## 4. 缓存策略（强制执行，不得自行发明）

| 缓存内容 | L1 Caffeine TTL | L2 Redis | 主动失效时机 |
|---|---|---|---|
| App 凭证信息（含权限） | 60s | 永久 | 凭证/系统状态变更时 |
| 模型配置（上游地址等） | 30s | 不存 Redis | 定时刷新（30s调度） |
| 访问规则 | 60s | 不存 Redis | 规则变更时 |
| 模型授权 | 60s | 永久 | 授权变更时 |

**降级规则：** Redis 故障时，已在 L1 缓存中的凭证继续放行；新请求（L1 miss）直接拒绝（不穿透到 DB 避免雪崩）。

---

## 5. 数据库操作约束

- **必须**使用 MyBatis Plus `BaseMapper` + `LambdaQueryWrapper`，查询逻辑在 Service 层或 Mapper 接口方法中
- **禁止**编写 XML Mapper 文件（所有查询用 Lambda API 表达）
- **禁止**使用原生 `JdbcTemplate` / `NamedParameterJdbcTemplate`
- **禁止**在 Service 层手写 SQL 字符串
- **必须**启用 `map-underscore-to-camel-case: true`（下划线自动转驼峰，无需 `@TableField`）
- **必须**使用 `@TableName` 标注实体对应表名，`@TableId(type=IdType.AUTO)` 标注主键

---

## 6. 安全约束

| 规则 | 实现要求 |
|---|---|
| 密钥加密存储 | 使用 `CryptoService`（AES-256-GCM），密钥从环境变量读取 |
| API Key 存储 | DB 中存 `SHA-256(api_key)`，用于校验；原文仅创建时响应 |
| AK/SK 存储 | AK 明文，SK 加密存储 |
| 字符串比较 | API Key 比对必须使用常量时间算法（`MessageDigest.isEqual`）|
| 日志脱敏 | 日志中禁止打印完整的 API Key / SK |

---

## 7. WebFlux 约束（Spring Cloud Gateway 是响应式框架）

- **所有 Filter** 必须返回 `Mono<Void>`，不得阻塞线程
- **Controller 中的 DB 操作** 必须切换到 `Schedulers.boundedElastic()` 线程池
- **禁止**在 GlobalFilter 中调用同步 JDBC，必须包裹在 `Mono.fromCallable(...).subscribeOn(Schedulers.boundedElastic())` 中
- **禁止**在 Filter 内直接使用 `Thread.sleep()` 或其他阻塞调用
